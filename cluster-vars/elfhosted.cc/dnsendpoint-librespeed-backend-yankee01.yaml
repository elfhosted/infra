apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "yankee01-speed.${dns_domain}"
  namespace: librespeed
spec:
  endpoints:
  - dnsName: "yankee01-speed.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "yankee01.${dns_domain}"