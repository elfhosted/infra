apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: chain-public-stremio-addon-stream
  namespace: traefik
spec:
  chain:
    middlewares:
    - name: error-pages
    - name: rate-limit-public-stremio-addon-stream