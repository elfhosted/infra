apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: chain-hosted-stremio-addon
  namespace: traefik
spec:
  chain:
    middlewares:
    - name: error-pages
    - name: inflightreq-10        # 10  x the hosted limit
    - name: rate-limit-10-per-1s  # 100 x the hosted limit