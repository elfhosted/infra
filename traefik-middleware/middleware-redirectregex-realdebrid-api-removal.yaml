apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirectregex-realdebrid-api-removal
  namespace: traefik
spec:
  redirectRegex:
    regex: ^realdebrid/.*
    replacement: https://deprecations.elfhosted.com/realdebrid-api-removal.json
