apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: redirectregex-annatar-stream-is-deprecated
  namespace: traefik
spec:
  redirectRegex:
    regex: ^.*/stream/.*
    replacement: https://deprecations.elfhosted.com/annatar-is-deprecated.json
