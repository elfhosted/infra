apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: inflightreq-10
  namespace: traefik
spec:
  # Global limit of 10 requests inflight concurrently
  inFlightReq:
    amount: 10