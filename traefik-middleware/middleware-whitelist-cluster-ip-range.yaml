apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: whitelist-cluster-ip-range
  namespace: traefik
spec:
  ipWhiteList:
    sourceRange:
      - 10.0.0.0/8 # larger than you'd think, but it covers csi-rclone mounting zurgs