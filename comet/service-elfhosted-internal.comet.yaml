apiVersion: v1
kind: Service
metadata:
  name: elfhosted-internal
  namespace: comet
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8000
  selector:
    app.kubernetes.io/name: comet
  sessionAffinity: None
  type: ClusterIP