apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: comet-warp
  namespace: comet
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 100Mi
  storageClassName: topolvm-provisioner-thin