apiVersion: v1
kind: ConfigMap
metadata:
  name: homer-config
  namespace: homer
data:
  config.yml: |
    ---
    # Homepage configuration
    # See https://fontawesome.com/icons for icons options

    # Optional: Use external configuration file.
    # Using this will ignore remaining config in this file
    # externalConfig: https://example.com/server-luci/config.yaml

    subtitle: "ci-elpenguino.net's"
    title: "Kube Cluster Dashboard"
    documentTitle: "ci-elpenguino.net Kube Cluster" # Customize the browser tab text
    # Alternatively a fa icon can be provided:
    icon: "fas fa-globe"

    header: true # Set to false to hide the header
    footer: true

    columns: "3" # "auto" or number (must be a factor of 12: 1, 2, 3, 4, 6, 12)
    connectivityCheck: true # whether you want to display a message when the apps are not accessible anymore (VPN disconnected for example)

    # Optional theming
    # theme: default # 'default' or one of the themes available in 'src/assets/themes'.

    # Optional custom stylesheet
    # Will load custom CSS files. Especially useful for custom icon sets.
    # stylesheet:
    #   - "assets/custom.css"

    # Here is the exhaustive list of customization parameters
    # However all value are optional and will fallback to default if not set.
    # if you want to change only some of the colors, feel free to remove all unused key.
    colors:
      light:
        highlight-primary: "#222526"
        highlight-secondary: "#4285f4"
        highlight-hover: "#5a95f5"
        background: "#222526"
        card-background: "#ffffff"
        text: "#363636"
        text-header: "#ffffff"
        text-title: "#303030"
        text-subtitle: "#424242"
        card-shadow: rgba(0, 0, 0, 0.1)
        link-hover: "#363636"
        background-image: /assets/backgrounds/kubernetes-background-5.jpeg
      dark:
        highlight-primary: "#222526"
        highlight-secondary: "#4285f4"
        highlight-hover: "#5a95f5"
        background: "#222526"
        card-background: "#2b2b2b"
        text: "#eaeaea"
        text-header: "#ffffff"
        text-title: "#fafafa"
        text-subtitle: "#f5f5f5"
        card-shadow: rgba(0, 0, 0, 0.4)
        link-hover: "#ffdd57"
        background-image: /assets/backgrounds/kubernetes-background-5.jpeg


    # Optional navbar
    # links: [] # Allows for navbar (dark mode, layout, and search) without any links
    links:

      - name: "Global"
        icon: "fas fa-globe"
        url: "#config"
      - name: "Cluster"
        icon: "fas fa-server"
        url: "#cluster"
      - name: "Controller"
        icon: "fas fa-computer"
        url: "#controller"  
        
      - name: "Dev"
        icon: "fas fa-flask-vial"
        url: "#dev"  
      - name: "Prod"
        icon: "fas fa-briefcase"
        url: "#prod"        

    # Services
    # First level array represents a group.
    # Leave only a "items" key if not using group (group name, icon & tagstyle are optional, section separation will not be displayed).
    services:
      - name: "Cloud"
        icon: "fas fa-cloud"
        # A path to an image can also be provided. Note that icon take precedence if both icon and logo are set.
        # logo: "path/to/logo"
        items:
        - name: "AlertManager"
          icon: "assets/png/aletmanager.png"
          url: "https://alertmanager.infra.${dns_domain}"
          target: "_blank" # optional html tag target attribute
        - name: "Grafana"
          icon: "assets/png/grafana.png"
          url: "https://grafana.infra.${dns_domain}"
          target: "_blank" # optional html tag target attribute                  
        - name: "Prometheus"
          icon: "assets/png/prometheus.png"
          url: "https://prometheus.infra.${dns_domain}"
          target: "_blank" # optional html tag target attribute
        - name: "Traefik"
          icon: "assets/png/traefik.png"
          url: "https://traefik.infra.${dns_domain}"
          target: "_blank" # optional html tag target attribute   