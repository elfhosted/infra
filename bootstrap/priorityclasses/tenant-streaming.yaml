apiVersion: scheduling.k8s.io/v1
kind: PriorityClass
metadata:
  name: tenant-streaming
value: 5000
globalDefault: false
description: "Used for tenant streaming pods, which should be kept running at the expense of regular pods"