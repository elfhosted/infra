apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: demo-accept
  namespace: demo-booklore
spec:
  forwardAuth:
    address: http://demo-terms:8080/auth
    trustForwardHeader: true
    authResponseHeaders:
      - "X-Forwarded-User"