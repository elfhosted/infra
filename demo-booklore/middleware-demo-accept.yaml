apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: demo-accept
  namespace: demo-booklore
spec:
  forwardAuth:
    address: http://booklore:6060
    trustForwardHeader: true
    authResponseHeaders:
      - "X-User"