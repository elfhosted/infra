apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "wildcard.nextpvr.${dns_domain}"
  namespace: dnsendpoints
spec:
  endpoints:
  - dnsName: "*.nextpvr.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "traefik-ingress.${dns_domain}"     
