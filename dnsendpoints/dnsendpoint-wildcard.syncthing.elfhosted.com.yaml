apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "wildcard.syncthing.${dns_domain}"
  namespace: dnsendpoints
spec:
  endpoints:
  - dnsName: "*.syncthing.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "traefik-ingress.${dns_domain}"     
