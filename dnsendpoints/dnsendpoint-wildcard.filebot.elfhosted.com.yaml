apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "wildcard.filebot.${dns_domain}"
  namespace: dnsendpoints
spec:
  endpoints:
  - dnsName: "*.filebot.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "traefik-ingress.${dns_domain}"     
