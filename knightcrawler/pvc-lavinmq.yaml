apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lavinmq
  namespace: knightcrawler
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block-ssd