apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: slooww.${dns_domain}
  namespace: slooww
spec:
  routes:
    - match: Host(`slooww.${dns_domain}`)
      kind: Rule
      services:
        - name: slooww
          kind: Service
          port: 80
