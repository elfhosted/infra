apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "opencost.infra.${dns_domain}"
  namespace: opencost
spec:
  endpoints:
  - dnsName: "opencost.infra.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "nginx-ingress.infra.${dns_domain}"