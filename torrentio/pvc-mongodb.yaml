apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb
  namespace: torrentio
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-filesystem-ssd