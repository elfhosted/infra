apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mongodb
  namespace: torrentio
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: ceph-block-ssd