apiVersion: traefik.containo.us/v1alpha1
kind: IngressRouteTCP
metadata:
  name: torrentio
  namespace: torrentio
spec:
  entryPoints:
    - postgresql
  routes:
  - match: HostSNI(`*`)
    services:
    - name: torrentio-rw
      port: 5432