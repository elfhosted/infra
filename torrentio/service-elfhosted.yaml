apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: traefik
  name: elfhosted
  namespace: torrentio
spec:
  ClusterIP: 10.43.0.8
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 7001
  selector:
    app.kubernetes.io/name: torrentio-addon-internal
  sessionAffinity: None
  type: ClusterIP