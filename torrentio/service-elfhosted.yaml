apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: traefik
  name: elfhosted
  namespace: torrentio
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 7001
  selector:
    app.kubernetes.io/name: torrentio-addon
  sessionAffinity: None
  type: ClusterIP