apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: elasticsearch
  namespace: zilean
spec:
  chart:
    spec:
      chart: app-template
      version: 0.2.1
      sourceRef:
        kind: HelmRepository
        name: elfhosted
        namespace: flux-system
  interval: 15m
  timeout: 5m
  releaseName: elasticsearch
  values:
    global:
      nameOverride: elasticsearch
    controller:
      replicas: 1
      strategy: Recreate      
    image:
      repository: elasticsearch
      tag: 8.15.0@sha256:310b9fc03b0657b0708c22b13cf3920f424c482a23f16a439d93c89636cb564c
    securityContext:
      seccompProfile:
        type: RuntimeDefault
      readOnlyRootFilesystem: false
    podSecurityContext:
      fsGroup: 1000
      seccompProfile:
        type: RuntimeDefault
    deploymentStrategy:
      type: Recreate
      rollingUpdate: null        
    env:
      ES_SETTING_DISCOVERY_TYPE: single-node
      ES_SETTING_XPACK_SECURITY_ENABLED: "false"
      ES_SETTING_BOOTSTRAP_MEMORY__LOCK: "true"
      ES_JAVA_OPTS: "-Xms512m -Xmx512m"      
    ingress:
      main:
        enabled: false 
    service:
      main:
        enabled: true # necessary for probes
        ports:
          http:
            port: 9200   
    persistence:
      config:
        enabled: true
        type: custom
        mountPath: /usr/share/elasticsearch/data
        volumeSpec:
          persistentVolumeClaim:
            claimName: elasticsearch    