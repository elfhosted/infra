apiVersion: v1
kind: Service
metadata:
  name: elfhosted-internal
  namespace: zilean
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8181
  selector:
    app.kubernetes.io/name: zilean
  sessionAffinity: None
  type: ClusterIP