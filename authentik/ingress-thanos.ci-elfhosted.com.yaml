apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: thanos.ci-elfhosted.com
  namespace: authentik
spec:
  ingressClassName: nginx
  rules:
  - host: thanos.ci-elfhosted.com
    http:
      paths:
      - backend:
          service:
            name: authentik
            port:
              name: http
        path: /
        pathType: Prefix