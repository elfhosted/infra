apiVersion: v1
kind: ConfigMap
metadata:
  name: librespeed-env
  namespace: librespeed
data:
  TITLE: ⚡️ ElfHosted Speed Test ⚡️
  TELEMETRY: "true"
  ENABLE_ID_OBFUSCATION: "true"
  REDACT_IP_ADDRESSES: "true"
  DISTANCE: "km"
  # DB_TYPE: "postgresql"
  # DB_HOSTNAME: librespeed
  # DB_NAME: librespeed
  # DB_USERNAME: librespeed
  EMAIL: legolas@elfhosted.com
  WEBPORT: "8000"