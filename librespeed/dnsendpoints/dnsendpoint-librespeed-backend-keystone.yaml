apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "keystone01-speed.${dns_domain}"
  namespace: librespeed
spec:
  endpoints:
  - dnsName: "keystone01-speed.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "keystone01.${dns_domain}"