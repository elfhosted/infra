apiVersion: v1
kind: Service
metadata:
  name: mediafusion-prowlarr
  namespace: mediafusion
  labels:
    app.kubernetes.io/name: prowlarr
  annotations:
spec:
  type: ClusterIP
  clusterIP: 10.43.0.6
  ports:
  - port: 9696
    targetPort: 9696
    protocol: TCP
  selector:
    app.kubernetes.io/name: prowlarr