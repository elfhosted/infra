apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: "mediafusion.${dns_domain}"
  namespace: mediafusion
spec:
  endpoints:
  - dnsName: "mediafusion.${dns_domain}"
    recordTTL: 180
    recordType: CNAME
    targets:
    - "traefik-ingress.${dns_domain}"